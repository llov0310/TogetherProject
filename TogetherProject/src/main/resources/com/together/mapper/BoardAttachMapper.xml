<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="org.zerock.mapper.BoardAttachMapper"> -->
<mapper namespace="com.together.mapper.BoardAttachMapper">

	<insert id="insert" parameterType="com.together.domain.AttachFileDTO">
		<![CDATA[
		insert into 
		tbl_attach(uuid,uploadpath,filename)
		values 
		(#{uid},#{path},#{fileName})
		]]>
	</insert>

	

</mapper>